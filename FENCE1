#include<stdio.h>
#define pi 3.141592
int main()
{
        double a;
        scanf("%lf",&a);
        while(a!=0)
        {
            double r=a/pi;
            double area=pi*r*r/2.0;
            printf("%.2lf\n",area);
            scanf("%lf",&a);
        }
        return 0;
}
